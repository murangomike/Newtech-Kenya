# Server
PORT=4000
JWT_SECRET=change-this-to-a-long-random-secret

# SMTP — Spacemail config
SMTP_HOST=mail.spacemail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=m.murango@newtechkenya.com
SMTP_PASS=your-spacemail-password
SMTP_FROM=NewTech Kenya <m.murango@newtechkenya.com>

# Alternative port (if 465 blocked):
# SMTP_PORT=587
# SMTP_SECURE=false
